const utils = {
    border: function(color, width=2) {
      return {
        borderWidth: width,
        borderColor: color,
      };
    },
};

export default utils;

// export default class utils {
//   generateBorder(color) {
//     return {
//       borderRadius: 1,
//       borderColor: color,
//     };
//   };
// }
